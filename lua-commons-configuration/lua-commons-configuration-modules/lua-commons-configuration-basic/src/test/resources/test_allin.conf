test_type = type("Hello, from Lua")
test_bit32_bnot = bit32.bnot(56)
test_math_floor = math.floor(17.7)
test_string_sub = string.sub("This test is succeeded, or no?", 14, 22)
local l = {"a", "b", "c", "d"}
test_table_concat = table.concat(l, "&")
